apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpo-backend-chart.fullname" . }}
  labels:
    {{- include "cpo-backend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpo-backend-chart.selectorLabels" . | nindent 4 }}
